version: 2

sources:
  - name: internal_production
    description: "External Parquet files loaded via staging views."
    meta:
      external: true
    tables:
      - name: users
      - name: drivers
      - name: merchants
      - name: transactions
